yf roadmap
this is NOT for you, just me

what we have now:
	pixel processing unit (yay!)
	memory (holding strong so far)
	lua VM (cpu) 
	palette (done in code)
	system font (remap special characters)
	audio processing unit (including tracker)
	kbd input (ill map controllers later)
	api/polished vm (though subject to rewrite)
	cart format (not fully tested but works for the most part)
	
what needs to be done:
	2 controllers possibly
	rewrite (maybe)
	file finder + project tools
	
whats the goal:
	for now, make it a framework like cel7
	for later, make a mini dev environment
