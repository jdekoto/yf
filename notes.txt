# notes

a. the SRAM doesnt load back up when running a Cassette
b. finish ant thoroughly test the apu sequencer
c. make the following cli tools
	rom2inst - rips the bytecode out from the cassette [nim]
	img2iraw - takes a bmp/png and RGB values with a indexed .raw [nim]
	json2map - takes a map in spritefusion json format and packs into a bin
	aputools - a terminal interface that controls everything that the apu will deal wit
	
