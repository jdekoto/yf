if you are reading this, then u must've got ahold of the demoscenes' source.
in here i basically was talking abou an inlcusions keys that u put in config.txt
but that was because of our cassette format, which was using the tar archive format.
but we have no use for it anymore so yayyy. so now the cassette format is a 84 byte
header followed by a bytecode binary.
